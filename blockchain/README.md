# CyberTrace Evidence Registry

`EvidenceRegistry.sol` stores only a case identifier, evidence hash, timestamp and registrar. Deploy to a local/test Ethereum-compatible network. Do not put raw email content or passwords on-chain.
